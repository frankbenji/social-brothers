import { SxProps } from "@mui/material";

export const styles = {
	container: {
		height: "32px",
		backgroundColor: "#262626",
		justifyContent: "center",
		alignItems: "center",
		mt: "auto",
	} satisfies SxProps,
};
