import { SxProps } from "@mui/material";

export const styles = {
	container: {
		display: "flex",
		flexDirection: "column",
		p: { xs: 0 },
		minHeight: "100vh",
		mx: "auto",
	} satisfies SxProps,
};
