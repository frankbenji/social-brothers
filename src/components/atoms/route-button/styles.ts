import { SxProps } from "@mui/material";

export const styles = {
	button: {
		borderRadius: 0,
		p: 0,
		pb: 1,
		minWidth: 0,
		alignItems: "flex-start",
		transition: "border-bottom 0.3s ease",
	} satisfies SxProps,
};
